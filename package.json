{
  "name": "f2i",
  "version": "1.0.0",
  "description": "mongo db project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 kill && pm2 start index.js --watch && pm2 logs"
  },
  "author": "msek",
  "license": "ISC",
  "dependencies": {
    "argon2": "^0.30.2",
    "express": "^4.18.2",
    "mongoose": "^6.7.2"
  }
}
